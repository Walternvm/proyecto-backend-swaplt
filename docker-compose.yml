version: "3.8"

services:
  postgres:
    image: postgres:16-alpine
    container_name: swaplt-db
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: swaplt-db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres